{
  "name": "liquid-resilience",
  "version": "1.0.0",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "format": "forge fmt",
    "format:check": "forge fmt --check",
    "lint:sol": "solhint \"./{src,test}/**/*.sol\"",
    "lint:slither": "python -m slither . --config-file slither.config.json"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "solhint": "^3.6.2"
  }
}
